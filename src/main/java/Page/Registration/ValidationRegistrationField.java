package Page.Registration;

import Page.BasePage;
import org.openqa.selenium.WebDriver;

public class ValidationRegistrationField extends BasePage {

    public ValidationRegistrationField(WebDriver driver) {
        super(driver);
    }
}
